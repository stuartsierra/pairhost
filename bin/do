#!/bin/sh

cd `dirname $0`/..
source bin/env.sh

if [ -z "$1" ]; then
    echo "Usage: bin/do <some-ec2-command>"
    echo "This will run the command in the correct environment"
    exit -1
fi

echo ""
echo "Running $@"
$@
echo ""
